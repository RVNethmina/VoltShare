# Base address of the VoltShare Web API during development.
# The API is hosted on IIS, so the development site talks to the same service
# the deployed site uses.
VITE_API_BASE_URL=http://localhost:8080/api/v1
