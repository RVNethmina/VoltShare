# Base address of the VoltShare Web API for the deployed build.
# The machine address is used rather than localhost so the site also works when
# it is opened from another device on the network.
VITE_API_BASE_URL=http://192.168.1.190:8080/api/v1
